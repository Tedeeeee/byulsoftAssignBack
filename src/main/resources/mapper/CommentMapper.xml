<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project.assign.mapper.CommentMapper">
    <resultMap id="commentResultMap" type="project.assign.entity.Comment">
        <id property="commentId" column="comment_id"/>
        <result property="memberId" column="member_id"/>
        <result property="boardId" column="board_id"/>
        <result property="memberNickname" column="member_nickname"/>
        <result property="commentContent" column="comment_content"/>
        <result property="commentCreatedAt" column="comment_created_at"/>
        <result property="commentUpdatedAt" column="comment_updated_at"/>
    </resultMap>

    <insert id="saveComment" parameterType="project.assign.entity.Comment">
        insert into comment
        (
         board_id,
         member_id,
         member_nickname,
         comment_content,
         comment_is_delete
        ) values (
          #{boardId},
          #{memberId},
          #{memberNickname},
          #{commentContent},
          false
        )
    </insert>

    <select id="findByCommentId" resultMap="commentResultMap">
        select comment_id,
               member_id,
               board_id,
               member_nickname,
               comment_content,
               comment_created_at,
               comment_updated_at
        from comment
        where comment_id = #{commentId} and comment_is_delete = 0;
    </select>

    <select id="findByBoardId" resultMap="commentResultMap">
        select comment_id,
               member_id,
               board_id,
               member_nickname,
               comment_content,
               comment_created_at,
               comment_updated_at
        from comment
        where board_id = #{boardId} and comment_is_delete = 0;
    </select>

    <update id="deleteByCommentId">
        update comment
        set comment_is_delete = true
        where comment_id = #{commentId};
    </update>

    <update id="changeComment">
        update comment
        set comment_content = #{commentContent}
        where comment_id = #{commentId}
    </update>

</mapper>